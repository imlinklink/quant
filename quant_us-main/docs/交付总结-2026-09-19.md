# 交付总结（2026-09-19）

本文件是**本批交付**的汇总，供 review 用。逐条实施记录在设计文档
`docs/llm-full-loop-decision-design-2026-09-19.md` §19（按时间追加）。

---

## 0. 先说明改动边界（review 前必读）

> **⚠ 本节已被 §9 取代。** 下面描述的"工作区混有两轮未提交改动"是当时的状态；
> §9 那一批结束时工作区**已干净**，全部改动按 11 个提交落地。本节保留作溯源：
> 它说明了 §1–§8 的边界，读 §1–§8 时仍应按它的口径区分"本批"与"更早一轮"。

工作区里**混有两轮未提交的改动**：

| | 内容 | 是否本批 |
|---|---|---|
| 更早一轮（同日早先） | P0 批次：`decision_contracts.py`、三个 `*_counterfactual.py`、`decision_engine` 的修复尝试、Entry 路由等 | ❌ 不是本批 |
| **本批** | 见第 1 节 | ✅ |

`git diff` 里 `mutifactor/llm/contracts/entry_v2.py`、`proposal_store.py`、
`dip_buy_monitor.py`、`web/app.py`、`run_daily_selection.py`、`signal_context.py`、
`option_view.py`、`run_suggestions.py`、`trend_breakout_monitor.py`、`position_review.py`、
`project_decision_metrics.py`、`workflow.py`、`evidence_packet.py` 等**不是我改的**，
请按"更早一轮"审。

**本批新增的源文件（11 个）**：
```
mutifactor/llm/contracts/portfolio_v1.py
mutifactor/llm/contracts/review_v1.py
scripts/portfolio_shadow/overlay_review.py
scripts/portfolio_shadow/position_overlay.py
scripts/portfolio_shadow/position_packet.py
scripts/portfolio_shadow/position_review.py
scripts/portfolio_shadow/portfolio_review.py
scripts/live_trading/decision_ledger/promotion_review.py
scripts/live_trading/decision_ledger/protocol_changes.py
scripts/live_trading/decision_ledger/review_stats.py
scripts/live_trading/protocol_review.py
```

**本批新增的测试（9 个文件，172 条）**：
`test_position_overlay.py`、`test_position_cli.py`、`test_portfolio_caller.py`、
`test_portfolio_review_roles.py`、`test_review_stats.py`、`test_protocol_review.py`、
`test_permission_promotion.py`、`test_evidence_claims.py`、`test_outcome_settlement_guard.py`

**本批修改的既有文件（13 个，约 +1149 / −350）**：
`portfolio_shadow/{cli,entry_review,llm_overlay,paper_engine,replay,report,schema,store}.py`、
`live_trading/{llm_permission,outcome_scheduler,position_registry,preflight_check}.py`、
`validators/action.py`、`config.yaml`，以及 `decision_engine.py` /
`decision_bridge.py` / `decision_run_store.py` / `outcome_jobs.py` /
`validators/evidence.py` / `selection_v4.py`（这几个与更早一轮重叠，我只动了其中几处，
第 8 节列了具体位置）。

**未提交**：全部改动都在工作区，没有 commit。

---

## 1. 交付范围：设计 §6 的五个角色

| 角色 | 本批交付 | 状态 |
|---|---|---|
| **Selection** | 提示词证据 ID 闭集**按股票分立**（原本是跨股票扁平合并，与校验器口径矛盾） | ✅ 可用 |
| **Entry** | 证据归属守卫放宽 + 归属构成披露 | ✅ 可用 |
| **Position** | 连续持仓路径：引擎新增 2.5 阶段、tier 相对应用、评审包/编排/CLI、结算接线 | ✅ 可用 |
| **Portfolio** | 契约 + **程序生成的合法模板** + 校验 + 包构造 + 调用方三段式 + 进 run-daily | ✅ 可用（默认关闭） |
| **Review** | 契约 + 统计构造器 + 排期 + 进服务定时器 + **人工批准入口** + 协议候选账本 | ✅ 可用（默认关闭） |
| §8/§12 | 权限等级配置 + 严格校验（接进启动自检）+ 晋级资格判定（只读、可审计、含未达标原因） | ✅ 可用（不得切换等级） |

**一句话**：五个角色都有契约、事件、指标、权限判定与调用路径；Portfolio 与 Review
**默认关闭**。开启后的行为性质两者不同，必须分开说：
- **Portfolio**：有**证明**——权限为 `shadow` 时 `effective_action` 是父策略，开启与不开启
  得到**同一个 `state_hash`**（另有反证证明筛选确实接上了）。
- **Review**：**构造性保证**——`may_affect_shadow_path=False` 且动作集合只有
  `{propose_change, no_change}`，没有通向配置写入的路径。这不是"运行后没发现影响"，
  是"不可能有影响"。

---

## 2. 关键口径（含**三处我推翻自己**的地方）

### 2.1 覆盖设计文档措辞的两条

1. **R 的定义**：设计 §6.4 写"回放实际机械追踪止损"。实测**影子引擎里没有追踪止损**
   （`stop_micro` 入场后只在拆股与分红两处变动；出场只有跳空止损/日内硬止损/时间退出）。
   ⇒ 本批的比较对象是"**LLM 持仓管理 vs 影子账户既有规则路径**"，§6.4 的措辞在影子落点下
   不成立。已写进 §19 与项目记忆。
2. **落点**：设计 §14 把连续路径放在 `decision_ledger`；按你的决定改为 `portfolio_shadow`，
   职责边界 = decision_ledger 管事件/证据/模型输出/归因，portfolio_shadow 管仓位/成交/净值。

### 2.2 我推翻自己的三处（请重点看）

| 我说过 | 实情 | 处置 |
|---|---|---|
| "选项 A（评审阶梯止损）偏差保守、不会美化 LLM" | **错**。R 止损比真实更松是**双向**的：跌穿后继续跌→R 成交更差→**美化 L**；跌穿后反弹→惩罚 L。符号不确定 | 改为按 `entry_mode` 分别解析；该措辞未进入文档 |
| "§7.1 的板块/风险组归属是待补缺口，要把它实现出来" | **错**。§7.1 原文"**其他证券或未声明板块一律拒绝**"——同板块**同行**的证券归属本就该拒。放宽它才是越界 | **不改**；新增 `test_evidence_claims.py`（12 条）把边界钉死，防下一次"顺手放宽" |
| "`subject_code` 缺失是待修 bug" | **已修**（§4.2 那次改动解决），我实测归属完整进包；失败快照产生于该修复之前 | 不改 |

### 2.3 一处你纠正过我的

**量化不保证"相差小于 1e-6 就落入同一档"** —— 落在档边界两侧的数仍不同。我原先把量化
当成了主要防线；按你的口径改为：**防重算为主、量化为辅**，并专门加测试证明"跨档仍触发
冲突保护"。

---

## 3. 修掉的一处正在发生的生产故障（结算链路）

**现象**：`live_trading/run_outcomes.py` 自 2026-09-19 11:29 起连续 `exit=1`：
```
{"status":"settlement_conflict","reason_code":"event_conflict","retryable": false}
```

**根因**（副本复现抓到确切差异）：
```
分歧行 1：VALUE_DIVERGED 1d research_batch_01ce7a1 delta=1.2803993976184813e-13
```
`excess_return_pct` 两次运行差 **1.28e-13**，而 `return_pct` **完全相同** ⇒ 个股收盘序列
一致，**基准（SPY）序列每次重取、浮点末位不同**。事件键固定 ⇒ `insert_event` 抛
「同一事件ID出现冲突内容」⇒ `retryable: false` ⇒ **日结算永久失败、永不重试**。
**一个没人会看的浮点末位把整条链路钉死了。**

**修复**：
1. **防重算（主）**：已结算的期限跳过。判据是**事件是否存在**，不是"投影说 `good`" ——
   该函数先在事务 A 写投影、再在事务 B 写事件；中间失败会留下"投影 good 但事件缺失"，
   跳过它就等于让账本永远缺这条记录。
2. **量化（辅）**：落库指标量化到 1e-6。
3. **显式修订** `revise_outcome`：唯一能改写已记录结果的入口，**不复用原事件键**（`rev{n}`），
   载荷带 `previous`/`reason`/`operator`，须人工调用。
4. **投影不一致的核查与修复**：`divergences()` 报出，`repair_projection` 只修 ≤1e-6 的
   （更大的**拒绝**并要求走 `revise_outcome`），修复留 `outcome_projection_repaired` 事件，
   **不动 `outcome_observed`**。
5. 顺带修正 `settled` 计数语义：原先数"算出了多少"（重跑也报 10 条），改为数"真写入了多少"。

**恢复方式（你要求查明的）**：`run_outcomes.main()` 失败**不持久化任何状态**；
服务侧 `ShadowJobs.execute` 的键是 `(job_key, 日期)` ⇒ **次日自动重新领取，无需清理**。
需处理的只有两件：① 那一条投影不一致的行（用 `repair_projection` 修）；
② 冲突处**整批中止**，那天之后的条目会在下次成功运行时补齐。**失败记录全部保留**。

---

## 4. 验证证据

| 验证 | 结果 |
|---|---|
| 全量测试 | **1266 passed**（本批开始时全量是 **1066**；新增 9 个测试文件，另在既有文件增补若干） |
| Selection 闭集按股票分立 | 跨股票引用被 **prompt schema 本身**拒掉（4 条测试） |
| Position 端到端 | 只有 MARKET 证据时产出 `POSITION_REDUCE_25`，L 125→94、R 保持 125 |
| Portfolio 开启后行为不变 | 开启/不开启得到**同一个 `state_hash`**；另有反证（空篮子确实剔除候选） |
| Review 统计（真实账本） | 1d 可用 59 / 3d 41 / **5d 0**；三条 caveat 已写进包 |
| 晋级判定（真实账本） | selection 输出有效率 **0.4**（4/10）；entry/position 无终态决策；三者均未达标 |
| 定时器接线 | 非交易日的周复盘不被 session 门挡住（专项测试） |
| 生产账本 | 本批**未向生产写入任何决策事件**（只读了账本；所有联调与验证都在副本上跑） |
| 结算链路修复 | 副本端到端验证**通过**（见下） |
| **联调：入场链路** | 24 会话历史重放**通过**（见下） |
| **联调：持仓连续路径** | 三层叠加逐日交织 24 会话**通过**（见下） |

### 4.1 联调（全部在副本上，未动生产）

**入场链路**——把副本清成空账本，按真实时序逐日重放 08-17→09-18：

| 环节 | 结果 |
|---|---|
| 候选 | 三个都出现在已知信号日：09-03 MU、09-15 AMD、09-16 LITE |
| 结算 | 每次 `due:1, intents_applied:2`（R、L 各建仓），`portfolio_dropped:0` |
| 重放一致性 | **R 与 L 双双 `match:true`** |
| 配对绩效 | `L−R 全成本收益差=0.00%`、`R MDD −0.91% == L MDD −0.91%` |

最后一行是**有判别力的**：夹具动作用 `PASS`（采用父策略），L 必须**逐字节等于** R；
差 0.00% 正是不变量成立的证据。L 侧若被错误筛掉/改动一个候选，这里会立刻显形。

**持仓连续路径**——另冻副本实验 `M1-ITR-POS-20260919`（打开 `position_overlay` 与
`portfolio_review`），入场 + 持仓 + 容量三层叠加逐日交织：

| 验的什么 | 结果 |
|---|---|
| 同一笔仓位**逐日连续评审** | `prepare-position` 冻结 1→2→3 个包；`review-positions` 的 `due` 1→2→3 |
| 动作被接受并应用 | 09-17 起 `path_changed: 2/3/3`（此前均为 0） |
| **L 的减仓是比例累积的** | R = AMD 20 / LITE 6 / MU 8；**L = AMD 12 / LITE 5 / MU 5** |
| 完整状态可复现 | **`replay` R 与 L 双双 `match:true`**（含连续路径的整个状态） |
| 容量层接上但未误伤 | 全程 `portfolio_dropped: 0`（权限 shadow 下应当如此） |

**L 那一行证伪了本批最初担心的那个缺陷**：若按冻结的绝对数量卖，连续四次 `reduce:25`
会减到 **100%**；实测 L 是 R 的 ~60%/83%，正是 `0.75ⁿ` 的比例累积。

**一个观察**：`llm_insufficient` 在 09-08…09-16 全为 1（无可用证据 ⇒ 弃权、**不调模型**），
**09-17 起为 0**、减仓才开始生效。原因是证据库里 MARKET 日报的 `observed_at` 是
09-17/09-18，更早的会话里它们属**未来证据**、被严格模式正确丢弃 ⇒ 包为空 ⇒ 质量门短路。
即**持仓评审在 09-17 之前结构上不可能产生动作**。实测符合预期，非 bug。

**联调中发现并修掉一个真 bug**：夹具把减仓档位的后缀（`':25'`）当成模板 id 传，
而真实 id 形如 `US.AAPL@2026-01-05:reduce:25` ⇒ 校验器判「模板不存在」⇒ 降级 ABSTAIN。
后果是 `review-positions --fixture-action reduce_25` **从来就没生效过、且不报错**——
持仓路径的夹具演练一直是空转。**单元测试全绿看不出来**（单测传的是真实模板 id）。
已修（夹具按 `(action, tier)` 从包内解析）+ 4 条回归测试。

**联调覆盖不到**：真实模型调用（需活的决策窗口 + 自然候选）；`shadow_daily.sh` 本身
（硬编码生产目录且默认 `--model real`）；生产 manifest 的 `position_overlay` /
`portfolio_review` 仍是关的（打开需另冻新 experiment_id）。

**结算链路修复的副本逐项验证**（生产副本，未动生产）：

| 步骤 | 结果 |
|---|---|
| 修复前 | 分歧 1 行，`delta=1.2803993976184813e-13` |
| 跑 `run_outcomes` | `{"status":"partial","settled":111,"pending":523}`，**`exit=0`** —— 修复前是 `settlement_conflict / retryable:false / exit=1` |
| 跑完后 | 那条旧分歧**仍在**（1 行）—— 重算被跳过，**没有静默改写历史**，这正是要的性质 |
| `repair_projection` | 修复 1 行，拒绝 0 |
| 修复后 | 分歧 **0** 行；`outcome_projection_repaired` 1 条；**`outcome_observed` 仍为 627 条（原始记录一字未动）** |

`settled: 111` 本身也是证据：修复前任务在**第一个**冲突处整批中止，此后成熟的期限全部没结算；
现在它们补上了。这与"冲突处整批中止、后续条目下次运行补齐"的判断一致。

---

## 5. 未完成与已知限制

1. **P0 门槛验收仍未达成**：正式前向实验 `shadow_job_runs=0`，**从未有过一次真实模型决策**。
   设计文档把"至少一条 Entry 决策完整贯穿 packet→模型→校验→权限→L 路→outcome"定为
   P0 的验收门槛 —— 门槛未过，按文档自己的规矩不该开始 P1/P2。**我是在门槛未过的前提下
   推进 P1/P2 的**，这点必须写明。
2. **实盘决策层 0 条 entry / 0 条 position 决策**；11 条全是 selection，其中 6 条因证据引用
   问题判失败（本批修掉了其中最可解释的一类）。
3. **影子引擎缺风险组上限**：§6.3 要求"不得突破风险组上限"，而 manifest **未声明**
   `max_group_risk_bp` ⇒ 该维度无法执行（已写进包的 `limits_unavailable`，说明是"没声明"
   而非"没触发"）。总风险预算也是**推导值**而非声明值。
4. **§11.2 独立样本分组**：`primary_event_cluster` 未进 outcome 投影，只能回溯快照。
5. **Review 的人工批准入口只有 CLI**，无界面。
6. **§17 的 API key 未迁移**：`config.yaml` 仍是明文（未进 git）。本批把启动自检从 `ok`
   改为 `warn`，但**没有替你轮换**。
7. 五个角色的**权限等级仍全部 `shadow`**，且 §12 判定表明当前**没有任何角色具备晋级资格**
   （多项指标 `unavailable` → 刻意 fail-closed）。

---

## 6. 我在本批引入又修掉的问题（供 review）

| 问题 | 怎么发现的 |
|---|---|
| `normalize` 回填的键不在 schema 里（`additionalProperties: False`）⇒ **Portfolio/Review 在引擎里根本走不通**。我此前报"两个角色能跑"是不准确的 | 加**引擎级**测试时暴露。只测纯函数完全看不出来 —— 它只在"归一化后再校验"这个顺序下出现 |
| 两个包构造器**没有 `packet_id`**，而校验器按它绑包 ⇒ 绑定无从成立 | 同上 |
| `registry_for` 插在类方法**中间** ⇒ 其后所有方法变成该函数的嵌套函数，**不报语法错、只表现为属性凭空消失** | 全量测试以 **61 个失败**抓到 |
| 编辑 `cli.py` / `store.py` 时各误删一个换行 ⇒ 一处语法错误、一处缩进错误 | 全量测试 |
| 测试夹具用了**相同主键**，第二条 `INSERT OR REPLACE` 把第一条替换掉 ⇒ 断言全错 | 单独调试时"通过"、pytest 下失败，对比后定位 |
| 测试夹具不自洽（已结算值 0.05 vs 破坏成噪声值）⇒ 断言算错 | 跑测试 |
| `_allocate` 把缺失的风险组上限按 **0** 处理 ⇒ 任何有风险组的候选都被拒 ⇒ **静默空仓而看起来像正常分配** | 写 Portfolio 调用方时读代码发现 |

**这些不是"测试很给力"的正面故事**，是我编辑与自检不够细致。它们都落在测试覆盖到的
地方才被抓到；同类问题若落在覆盖之外，就会变成下一个"机制看起来在工作、实际没有"。

### 6.1 复核四个验收修复时又发现的两处（已修）

你对上述问题做了四项修复后，我逐项复核（**结论：四项的实质内容都正确**，并在副本上
独立复现了 1257 passed 与 `git diff --check`），复核中发现两处新问题：

| 问题 | 性质 | 处置 |
|---|---|---|
| `ALREADY_HELD` 会触发 `consult_required=True` | **由"持仓计入"那项修复带出的副作用**：`conflict = any(reason != 'INSUFFICIENT_CASH')`，而 `ALREADY_HELD` 是新增的拒绝原因。后果是——一个**已持有**的标的再次成为候选时会发起一次**付费模型评审**，而 Portfolio 在那情形下**无可决策**（所有模板都会同样拒绝它，换序也改变不了什么）。这正是 §6.3「没有容量冲突时默认不调用」要避免的 | 把 `ALREADY_HELD` 与 `INSUFFICIENT_CASH` 一并列入 `NON_CONTESTING_REJECTIONS` |
| `repair_projection` 仍是**两次事务** | 同一项修复把 `revise_outcome` 改成原子提交，但 `repair_projection` 没跟上：`UPDATE` 在一个事务、`outcome_projection_repaired` 事件在另一个。崩在中间 → **投影已修好、修复记录没写**；而投影修好后分歧就消失了，下次调用没有东西可修 ⇒ **那条记录永久丢失**。恰好发生在一个"存在意义就是留下审计轨迹"的函数里 | 改为 `insert_event(con, make_event(...))` 同一事务；并把模块级 `insert_event`/`make_event` 提到顶层导入以便测试可注入 |
| 夹具把减仓档位的**后缀**当模板 id（`':25'`，真实 id 形如 `US.AAPL@…:reduce:25`）⇒ 校验器判「模板不存在」⇒ 降级 ABSTAIN。**`--fixture-action reduce_25` 从来就没生效过、且不报错**，持仓路径的夹具演练一直空转 | **联调时按链路走一遍发现的**——单元测试全绿看不出（单测传的是真实模板 id） | 夹具改为按 `(action, tier)` 从包内解析真实模板 id；+4 条回归测试（提交 `11a9eba`） |

各配一条测试（`ALREADY_HELD` 单独出现时 `consult_required=False`，并保留"位置用完/风险预算用完仍触发"的正向断言，避免把信号一并关掉；`repair_projection` 事件写失败时投影**不被改动**）。

**两条测试都验证过"没有修复时会失败"** —— 退回旧写法跑一次、确认失败、再还原（这是
必要的一步：两边都过的测试没有价值）。

全量 **1262 passed**（本轮 +5）。

---

## 7. 需要你拍板/留意的

1. **P0 门槛（唯一还开着的实质项）**：设计 §13 把"至少一条 Entry 决策完整贯穿
   packet→模型→校验→权限→L 路→outcome"定为放行 P1/P2 的前置，而生产实验至今
   `shadow_job_runs=0`。**它卡在数据上、造不出来**：08-31 月末选股挂着的 INTC/ARM
   至今未触发入场信号，等待窗到 **09-29**；否则要等 09-30 的月末选股（10-01 起新窗）。
   已就绪的部分见 §4.1——除了真实模型那一跳，全链路都验证过。
2. **生产故障**：代码已修 + 那条投影残迹**已在生产上修复完成**（操作人字段填的是
   `claude-code`，因为它会永久留在账本里，我没有编人类名字——如需更换请告诉我）。
   余下只有一件会自动发生的事：下次定时运行会把修复前被整批中止而漏结算的条目补齐。
   **注意**：`run_outcomes` 走子进程 ⇒ 修复无需重启即生效；但定时器相关的新代码
   （周复盘、`registry_for`、启动自检）是进程内的 ⇒ **需要重启服务才加载**。
3. **§17 的 API key**：需要你轮换并改为 `${DEEPSEEK_API_KEY}`（启动自检现在会告警）。
4. **提交**：已按**两个提交**落地——`a220cf0`（前一轮 P0/P1 批次）+ `a20a587`（本批），
   另有 `bcc24d9`（P0 观察）与 `11a9eba`（夹具修复）。**原计划的"五条线"不可行**：
   两轮改动在 6 个文件里同 hunk 交织、无法按行切开（§0 有证据）。
5. **P0 观察已就位**：搭在既有 LaunchAgent 日任务上（不新增调度器），状态行按日累积到
   `data/portfolio_shadow/p0-watch.log`，并在 `DECIDED`/`SETTLED`/`MODEL_FAILED`/
   `DEADLINE_MISSED`/`NO_EVIDENCE` 上弹通知。**请确认那条"通道测试"通知你看到了**
   ——launchd 上下文不一定弹得出来，看不到我就改成只写流水。
6. **证据时效**：证据库目前只有 MARKET 日报，`evidence_window_days: 30`。**若连续多日
   不发布日报，有候选也会变成 `NO_EVIDENCE`（模型不被调用）**——那会让 P0 看起来卡在
   模型上，实际卡在证据上。

---

## 8. Review 后修复（2026-09-19）

针对本次代码 review 的四项发现，已完成以下修复：

- `revise_outcome` 保留未指定的指标、标签时间与原始 body，接受显式字段修订；新事件保存完整前后值及修订前投影。事件与投影同事务提交，任一失败全部回滚。旧事件未记录的字段从原投影保留，不声称可以从旧事件恢复这些字段。
- `repair_projection` 对 return 与 excess 两个待覆盖指标均检查容差；缺失值与零严格区分，非有限值拒绝作为噪声修复；核查限定当前 account scope。
- 晋级独立样本按冻结决策的证券、主要事件簇、美东交易周去重，不再使用结果结算日期；Position 按整笔 trade 去重。多个事件簇没有明确主簇、缺少身份/时间/快照时标 unavailable，不猜测、不计入晋级资格。
- Portfolio 模板计入已有持仓数与总风险，避免重复分配已有证券。调用方按当前价格到保护线的潜在损失计算已有持仓风险，向上取整到基点；缺行情时使用持仓成本价作为估值回退。风险组上限缺失的原有限制仍存在。

新增 13 条回归测试，覆盖完整字段保留、显式修改、事务回滚、收益大幅分歧拒绝、缺失值、微小噪声修复、跨期限/跨周/整笔交易去重，以及已有持仓容量与风险传递。

验证：`python3 -m pytest tests/unit -q` → **1257 passed, 18 warnings**（69.75 秒）；`git diff --check` 通过。未运行生产修复命令，未提交代码。

---

## 9. 后续批次（2026-09-19 晚，已全部提交）

**§0 的「工作区混有两轮未提交改动」已经失效** —— 本节结束时工作区干净，全部改动按
11 个提交落地（`8df223d` 之后）：

| # | 提交 | 内容 |
|---|---|---|
| 1 | `15b1ff0` | 容量评审 `store.registry` 不存在 + CLI 级回归 |
| 2 | `5404485` | 常驻服务加 launchd KeepAlive |
| 3 | `70718ed` | 恢复运维定时任务（整块死在迁移前旧路径） |
| 4 | `1e7c2e1` | 盘前「选股建议」加回来（摘掉它的理由不成立） |
| 5 | `26f0e4e` | 晋级的越权判据对 portfolio/review 恒真 |
| 6 | `41df41b` | entry/position 真实模型路径拿不到有效决策（两处） |
| 7 | `221f158` | review 的变量方向约束没进包 |
| 8 | `1d00bf1` | Portfolio 接进实盘决策层 |
| 9 | `d8dbff0` | 删掉第三份角色清单 |
| 10 | `8953157` | 容量判定看不见在途提案 |
| 11 | `f544d1d` | 受约束买入缺 `max_positions` 检查 |

全量测试 **1257 → 1319 passed**。

### 9.1 版本口径的一处变化（影响后续所有判断）

**用户明确：不要因为 LLM 会付费而回避调用，需要调用时就调用。**
判断该不该调，看的是"它是不是该做的那件事"，不是花多少钱。

据此：crontab 盘前恢复为 `pipeline.py --mode morning`（含调 LLM 的选股建议），
并开启 `llm_decision.protocol_review`（原 `enabled: false`，理由写的就是"会发起付费调用"）。

### 9.2 主线一：三处「机制看起来在工作、实际没有」

| 现象 | 实情 |
|---|---|
| 服务静默死了 3.5 小时没人知道 | `run_all.py` 无任何守护；唯一会喊「服务不在线」的 `ops/watchdog.py` **挂在已失效的 crontab 上** |
| 运维定时任务"在跑" | 六条**全部**指向迁移前的 `~/Documents/quant`，日志总共几行全是 `Operation not permitted`；简报产物停在 09-05 |
| 容量判定"在工作" | 在途提案对**每一处**容量计算都不可见 ⇒ 谁赢由轮询顺序决定 |

第一处已装 launchd `KeepAlive`（实测 `kill -9` 后约 30 秒自动拉起；附带好处：
`launchctl list` 现在会显示上次退出信号 —— 17:40 那次缺的正是这个痕迹）。
第二处先**用探针验证了前提**（cron 现在能不能跑 `~/quant`），确认迁移顺带解除了
TCC 阻塞后才恢复。第三处见 §9.4。

### 9.3 主线二：契约没把「校验器强制的约束」告诉模型

真实模型跑持仓评审，返回了一个完全合理的 `hold`，却被判 `INVALID_OUTPUT` 降级 ABSTAIN。
追下去是**同一形态的三处**：

| 处 | 校验器强制 | 契约给的 |
|---|---|---|
| entry / position | `reason_codes` 必须来自 14 项注册表 | schema 里是**任意字符串** |
| entry / position | `fact` 必须逐字匹配证据摘要 | 契约**没有 normalize 钩子**（selection/portfolio/review 都有） |
| review | 每个可变更变量的**方向**白名单 | 包里只有变量名 |

**为什么测试全绿**：夹具路径**手工只发合法值**（`FakePositionModel` 的注释就写着
"给一个自造的会被校验器拒" —— 阅读时看到的是"已处理"，实际是"绕开了"），
且夹具摘要是一句话（真实是 6000 字日报，全等不可能）。

修完的端到端证据（真实模型，副本，实时窗口）：

```
修复前  reason_code=INVALID_OUTPUT   12 条校验错误  → POSITION_ABSTAIN
修复后  reason_code=THESIS_WEAKENED  0 条错误       → POSITION_HOLD
```

`path_changed` 两次都是 0，但含义相反：前者是"决策被丢弃"，后者是"模型判断该持有"。
**这是 Position 角色第一次产出有效真实决策。**

另有一条同类：`promotion_review.ROLE_BASELINE` 手写三项而执行侧有**五项** ⇒
`baseline=None` ⇒ 越权判据退化成「动作非空即越权」 ⇒ 完全正确的决策被记成硬风控越权，
**永久挡住晋级**。原测试 `for role, baseline in ROLE_BASELINE.items()` 只遍历小字典，
少掉的角色永远测不到。改成派生 + 双向断言后，**review 成为五个角色里第一个"具备晋级资格"的**。

### 9.4 Portfolio 接进实盘层 + 容量分配

**接进实盘**（`1d00bf1`）：此前 `decide_portfolio` 全仓只有影子实验一个调用方，实盘
`llm_decision_runs` 里该角色 0 条。新增 `portfolio_allocation.py`，照 `protocol_review.py`
的先例**直接构造 `DecisionEngine`**（不动 `DecisionRuntime.ROLES`）。触发点 = 买入提案评审时；
仅确有容量冲突才调模型，**同账户每天最多一次付费调用**。权限仍 `shadow` ⇒
生效动作恒为父策略 `keep_rule_allocation`，**实盘行为一字不变**（有测试钉死 + 反证）。

接线时又撞出两处**结构上不可达**：组上限只支持标量而实盘是**按组**的（传 `None` 则
`reduce_same_group_concentration` 永远生成不出来）；Portfolio 是**多证券**角色而校验器只认
单一 `subject_code`（候选自己的证据全被判跨股票 ⇒ 改变分配的模板必然无据可引）。

**容量分配**（`8953157`）：`risk_quantity` 与 `submit` 的 `occupied` 都只认 `book['orders']`，
而 `pending` 提案**不是订单**。修法两条线：容量预留看见在途提案（三处判断用**同一个集合**）；
超容量的按**规则序**（信号到达顺序）标 `skipped` 并写明位次与上限。
`submit` 里**只被排在本条前面的提案挡** —— 算上全部会互相阻塞，`max_positions=1` 且有 A、B
时双双被拒、一个都进不去（既有测试当场抓到）。

### 9.5 本批踩到并修掉的自伤（供 review）

| 坑 | 形态 |
|---|---|
| 嵌套事务死锁 | `active_buys()` 自己开事务，在 `registry.transaction()` 里调用 → `database is locked`。故 `reserved_slots` **改签名收列表而不是 store**（旧签名在邀请这个死锁） |
| `begin_review()` 对无 `plan_id` 的旧式提案直接返回 None | 拿它构造"评审在飞"的测试夹具会**假装在测、实际什么都没设** |
| 我只补了一个调用点的归一化 | `validate_position_output` 有**两个**调用方（记错误 / 定动作），只补后者会出现"记下的错误"和"据以判定的规则"不是同一套 |
| 第一版容量判据算上全部在途提案 | 互相阻塞、双双被拒；改为只算排在前面的 |
| 一个已经失效的假设 | 我怀疑监控器 dry-run 读错 namespace，**查证后不成立**（`ProposalStore` 与监控器用的是同一个模块级 `REGISTRY`） |

### 9.6 验证证据

- 全量 **1319 passed**；每处修复都验证过"**没有它会失败**"（逐条把修复退回去跑一遍）。
- **实盘账本副本 e2e**：容量对账 4 个候选 / 上限 3 → 恰好第 4 位标 `skipped`、
  note 为`容量未分配：规则序第 4 位，超出 max_positions=3`，前三位不动。
  Portfolio：`consult_required=True` → `validated` → `promotion_review` 里该角色从
  `unavailable` 变成 `output_validity 1.0`。**生产账本全程未动**。
- 运维：`kill -9` 后约 30 秒自动拉起；看护实跑 `✅ 全部通过`；
  `--print` 生成的 plist 与盘上已装的**逐字段相同**（日常作业没被动到）。

### 9.7 未完成与已知限制（更新 §5）

1. **P0 门槛仍未过** —— 卡在数据上：需要一个**在实时窗口内出现的自然候选**。
   **窗口现在就开着**（08-31 月末选股的等待窗 = 09-01…09-29 共 20 个 session，
   已过 13 个，还剩 09-21/22/23/24/25/28/29）。**不是"等到 09-28"**（那是窗口最后一天）。
   任何一天出现信号即可，到 09-18 为止没出现。
   - 实测（09-19 深夜）：对副本逐个 session 跑 `prepare`，09-15 有 1 个（AMD）、
     09-16 有 1 个（LITE）、**09-17 与 09-18 都是 0**。
   - **历史 session 不能替代**：`RealModel._refusal` 的判据是**截止时刻**而非哪个 session ——
     09-17 的执行日截止已过 50h、09-18 已过 26h，均超 6h 时效 ⇒ `HISTORICAL_AS_OF` 拒绝；
     而 09-21 的截止还有 46h ⇒ **不拒绝，现在就能发起真实调用**。那个拒绝是对的：
     现在发起的调用必然晚于历史截止，会被判 `LATE_RESPONSE` 丢弃，钱花了结果不能用。
     `historical_debug` 能放开这两道门，但它**刻意不写 Application**（否则等于用今天的
     结果补填前瞻记录），故不满足 P0。
   - 门槛里的 `outcome` 还有自己的期限（1/3/5/10/20 日）：首个候选入场后，**决策→L 路**
     立即可见，**完整贯穿含 outcome** 还要再等至少一个 session。
   本批把**除真实模型那一跳之外**的所有结构性阻塞都清了，门槛一旦有候选即可验。
2. **`config.yaml` 完全不在 git 里**：HEAD 那份 93 行（initial commit），本地 413 行且含
   明文 `api_key`，靠 skip-worktree 隐藏。**整个 LLM 配置没有版本历史** ——
   本批改的 `protocol_review.enabled/weekday` 与 `portfolio_review.enabled` **都无法提交**。
   这改变了 §17 的性质：它不只是安全项，**它挡着配置的可复核性**。
3. **`ops/` 零测试覆盖**；且本批暴露的失效形态是「代码是对的、**装上去的是旧的**」
   （`build_block()` 生成的路径一直对，只是迁移后没人重跑 `--install`）——
   能抓住这类漂移的是 `已安装 == 预期` 的核对，不是单元测试。
4. **Portfolio 的实盘触发今天是罕见的**：监控器前置于"持仓数"、且在途提案曾不被计入容量，
   所以"多个不同代码同时挂提案"这个窗口存在但很少被物化。§9.4 的修复让它变得**可见且确定**，
   但仍需真实候选才谈得上效果证据。
5. `submit_constrained_entry` 已补 `max_positions`，但该路径**当前仍被 `constrained_action`
   权限挡着、生产不可达**——补齐只是让文档与代码一致。
6. runbook §6 的重启命令仍是迁移前的旧路径（照抄会失败）。

---

## 10. 第二轮联调（2026-09-19 深夜）

本节覆盖 §9 之后的工作，**日期 2026-09-19**，6 个提交。出发点是「还能联调什么」，
结果又挖出三处「机制看起来在工作、实际没有」——与 §9 是同一个成色的东西。

### 10.1 六个提交

| # | 提交 | 内容 |
|---|---|---|
| 1 | `7511f71` | 测试往**生产**评估账本写入（signals.jsonl 九成是测试垃圾） |
| 2 | `64d861b` | 删掉服务已停用策略线的遗留作业 + 周报空小节显式说明 |
| 3 | `879c400` | 更正 P0 窗口的时间口径（"等到 09-28"是错的） |
| 4 | `b73dcbd` | 看护的告警通道**从未送达过** |
| 5 | `9f528e1` | 加「已安装 == 预期」核对，并让看护每 5 分钟自动跑它 |
| 6 | `643ca30` | 配置记录补到当前状态，并让它被自动核对 |

### 10.2 本轮的四处发现

**① 测试在往生产评估账本写入。** 跑恢复后的周报，输出的"最近提案样例"里出现 `US.A`
（测试代码）。追下去：`data/decision_ledger/signals.jsonl` **515 行里 466 行是 `US.A`（90%）**，
跨 11 天累积。根因是 `ledger.record()` 写**项目固定路径**，与调用方用哪个 registry 无关 ⇒
"建了 `ProposalStore` 却没 patch `_record_ledger`"的测试直接写进生产。
**而 `weekly_report.py` 读的正是这份 JSONL —— 周报一直在说谎。**
修法是结构性的（`QUANT_LEDGER_DIR` + `tests/conftest.py` autouse 守卫）：
**光靠每个测试自觉是防不住的**，下一个新建 `ProposalStore` 的测试照样会污染。
已清掉 471 行（备份在位）。

**② crontab 的 08:35 是遗留作业。** 它服务的抄底线已被买入主链取代
（`run_all.py:140` 明写"不再启动"、config `standalone_dip_buy: false`），
而 `scan_ledger.record_scan` 全仓**只有它一个调用方** ⇒ 扫描流水自 **09-13** 起没再增长，
而那对命令只回看 3 天 ⇒ **永远报「0 条」**。这不是"没事"，是"源头没了"，
两者在日志上一模一样。已删。

**③ 看护的告警通道从未送达过。** 看护在 09-19 报过警（服务静默死 3.5 小时那次正是它该喊的），
但那条通知**根本没有出现在你眼前**。实测：`osascript display notification` 返回 **rc=0、
stderr 为空**、`usernoted`/`NotificationCenter` 都在跑 —— 而**一条都没看到**；
对照 `display alert` **立即可见**（你点了按钮）。原因是前者要求发送方（osascript 的宿主
**Script Editor**）持有通知权限。**也就是说看护的整套告警是装饰性的**。
一并修掉三个同源问题：`display notification`→`display alert`（带 `giving up after`）、
**记录「有没有被人看到」**（原先那行 `[通知]` 写在 osascript **之前**，证明不了送达）、
**转义 AppleScript 字符串**（原先消息里只要有一个引号或反斜杠就破坏整条脚本，
而 `check=False` 把它吞干净）、**只在问题集合变化时弹**（原先每次失败都弹，
`--once` 无状态 ⇒ 持续问题会每 5 分钟弹一次模态框）。

**④ 我上一轮说错了一个口径。** 我写"P0 要**等到 09-28**"——**错了**，那是窗口的最后一天。
实测：窗口 = **09-01…09-29 共 20 个交易日**，已过 13 个，**还剩 7 个**，
**现在就开着**。并纠正一处容易被误读的机制：**历史 session 不能替代** ——
判据不是"哪个 session"而是**截止时刻**（`RealModel._refusal`）：09-17 的执行日截止已过 50h、
09-18 已过 26h，均超 6h ⇒ `HISTORICAL_AS_OF`；而 **09-21 的截止还有 46h ⇒ 不拒绝**。
那个拒绝是对的（现在发起的调用必然晚于历史截止、会被判 `LATE_RESPONSE` 丢弃、钱白花）。

### 10.3 新增的一致性核对（本轮交付的机制）

| 核对 | 比什么 | 反证 |
|---|---|---|
| `install_cron.py --check` | 已安装 crontab 块 vs `build_block()`（不一致打 unified diff） | 改一行 → 精确报出 |
| `install_launchd.py --check` | 已安装 plist vs `build_plist(job)`（逐键列"已安装/应为"） | 改 `StartInterval` → `999 vs 300` |
| `ops/check_config_record.py` | `config.yaml` vs `docs/llm-decision-settings.yaml` | 改 `weekday` → `配置=5 记录=4` |

**三者都挂进看护**，每 5 分钟自动跑 —— 因为**一个没人执行的核对与没有核对等价**，
而这正是本批反复出现的教训。去重保证只弹一次。

关于配置记录：用户明确「**config key 就用现在的，不轮换**」（§17 的迁移不做）。
但 §17 还有另一半后果 —— `config.yaml` 不在 git 里 ⇒ **它的改动没有版本历史**。
正解早就有：`docs/llm-decision-settings.yaml` 就是这份记录（"不含凭证"）、**它在 git 里**，
只是停在 09-09。已补到当前状态并写清"为什么改成这样"。

### 10.4 ⭐ 已验 / 未验 对照表

**这是本节最该看的一张表。** 「联调通过」有两层含义，本轮的结果是：
**代码路径大部分验过了，但"生产真的跑过"这一半几乎全没过** —— 因为生产上没有流量。

账本实情（09-19 深夜）：

```
实盘决策      entry: 0 条   position: 0 条   portfolio: 0 条
              selection: 11 条（5 validated / 6 failed）   review: 1 条（手动跑的）
确认页提案    0 个
影子实验      shadow_job_runs: 0        ← 定时日任务从未真正跑过
P0 观察流水   p0-watch.log: 0 行
```

**① 已验（证据充分）**

| 项 | 证据 |
|---|---|
| 容量分配（在途提案参与容量 + 规则序分配） | 单元 18 条 + 副本 e2e + 两处反证 |
| Portfolio 接进实盘层 | 副本 e2e（`output_validity 1.0`）+ 阴影不变量 + 反证 |
| entry/position 真实模型路径（原因码闭集、normalize 钩子） | **真实调用，0 条校验错误** |
| review 变量方向 / `ROLE_BASELINE` | 单元 + 反证 |
| selection 修复后 | 1/1 `validated` |
| `protocol_review` 排期路径 | 副本驱动真实 tick 走完全链路 |
| **看护告警通道** | **你亲眼确认（点了按钮）** |
| 三处漂移核对 | 反证都做了 |
| 影子日任务整脚本 | 副本跑通，P0 钩子写下第一行 |
| 服务 KeepAlive | `kill -9` → 约 30 秒自动拉起 |
| 恢复的 crontab 三条 | 手动逐条跑通 |

**② 未验（从未在生产发生过）**

| # | 项 | 为什么没验 |
|---|---|---|
| 1 | **P0 门槛本身** | `entry` 决策 **0 条** —— 没有候选，这是整件事的目标 |
| 2 | **实盘交易路径的真实流量** | `decision_proposals: 0` ⇒ 本轮改的 `execution.py`（规则序容量、受约束买入的 `max_positions`）**在生产上从未被触发过一次**，可信度完全建立在单元测试与副本上 |
| 3 | ~~影子实验定时日任务从未跑过~~ **【已更正，见 §10.6】** | 我查错了表 —— 仓库里有**两处同名**的 `shadow_job_runs`，影子日任务**确实跑过** |
| 4 | Portfolio / position 的实盘路径 | 各 0 条 |
| 5 | `submit_constrained_entry` | 被 `constrained_action` 权限挡着、生产不可达 |
| 6 | **所有定时器的自动触发** | crontab 3 条、`shadow_daily`（周二 16:40）、`protocol_review`（09-26 06:30 北京）都只**手动**跑过，没等到过一次自动调度 |

**准确的说法**：能验的都验了，但它们绝大部分是**在没有流量的路径上验的**。
真正意义上的"全部通过"，要等生产上**第一次有真实候选** —— 那是这些改动第一次被真实数据走一遍。

### 10.5 下一步

- **等**：周一 **09-21** 是候选窗口（09-01…09-29）里的下一个交易日；`run-daily` 17:40 北京自动跑。
- **等**：周二 **09-26 06:30 北京** 是 `protocol_review` 的第一次真实排期。
- **等**：周二 **16:40 北京** 是 `shadow_daily` 的生产真跑 —— P0 观察钩子第一次执行。
- **仅剩一件需要你**：§17 的 key 已决定不轮换；其余无需人工介入。

全量测试 **1319 passed**（`ops/` 不在测试范围内）。

### 10.6 更正 §10.4 的一处 + 三个作业的真实状态（2026-09-19 深夜追加）

**你在追问「要真金白银买一只股票吗」时，我发现自己 §10.4 写错了一条。**

**① 更正：影子日任务不是"从未跑过"。**
仓库里有**两处同名**的 `shadow_job_runs`：
- `portfolio_shadow` 的（入场/持仓评审的崩溃窗口守卫）—— 在**实验账本**里，
  我查的是它，得 0 ⇒ 只说对该实验「评审从未发生过」（没候选，属实）；
- 服务侧 `ShadowJobs` 的（selection / setup / outcomes 三个作业）—— 在 `data/execution.sqlite3` 里。

而**影子日任务确实跑过**：实验账本里 `shadow_account_state: 4`、`daily_nav: 4`、
`applications: 2`，已结算到 09-18。**§10.4 的原表述是错的，已就地标注更正。**

**② 服务侧三个作业的真实状态（比"没跑过"更该看）**

```
2026-09-14  selection_and_reconcile  attempt=1 → failed
2026-09-14  selection_outcomes       attempt=3 → failed   ← 重试耗尽
2026-09-16  （无任何记录）                                ← 服务当时没在跑
2026-09-17  （无任何记录）
2026-09-18  selection_and_reconcile  attempt=1 → succeeded
2026-09-18  daily_setup_shadow       attempt=3 → failed   ← 重试耗尽
2026-09-18  selection_outcomes       attempt=3 → failed   ← 重试耗尽
```

**`selection_outcomes` 从未成功过一次**（2/2 失败）。两个失败**根因都是今天已修的 bug**：
`daily_setup_shadow` 的失败输出里能看到 `prompt_version: selection-v4.2` 与
`validate_failed: … 证据缺少归属 / 引用不存在` —— 正是今天修掉的跨股票证据 ID 问题；
`selection_outcomes` 是今天修掉的结算链路浮点冲突。

**③ 新发现：重试耗尽后是**永久的洞**，而且没有任何东西报告它。**

`ShadowJobs.claim`：`if last['status'] != 'failed' or last['attempt'] >= max_attempts: return None`
⇒ `max_attempts=3` 用完后，**该 session 永远不再重试**。于是 09-14 与 09-18 的那几条作业
**永久缺失**，而系统里**没有任何一处**会报告"某个 session 的作业没跑完"。
这与本批其它几处是同一个形态：**一个缺口没有被任何东西看见**。

**④ 关于"要不要买真金白银"——不需要。**

| 路径 | 实际状态 |
|---|---|
| 实盘确认页 → 下单 | `live_manager.trd_env: SIMULATE` + `run_all.py --dry-run`（默认 `dry_run=True`）⇒ **不碰真钱**，虚拟成交 |
| 影子实验（P0 门槛所在） | R / L 都是**模拟账户**，纸面净值 |
| 因此缺的是 | **信号 / 候选**，不是资金 |

`decision_proposals: 0` 的原因是 **`rule_passed: 0`**（规则没放行过任何信号），
不是"没钱买"。所以这几项**不需要花一分钱**，要么等自然信号，要么在副本上用合成数据驱动。

**⑤ 已处置（2026-09-19 深夜追加，提交 `4684001` + `fb08976`）**

- **根因之一找到了**：`run_daily_setups.py` 的退出码写的是
  `0 if all(quality=='pass') else 1` —— **只要一个标的不合格就 exit 1**，而 `ShadowJobs`
  把非零当**作业失败** ⇒ 重试 3 次（同一原因必然同样失败）⇒ **永久不再补**。
  实测 `US.RAM` 撞的是 `IMMUTABLE_SNAPSHOT_CONFLICT`（重跑过去 session 不能覆盖已冻结
  快照，**正当拒绝**）—— **一个正常的拒绝被退出码放大成"作业永久失败"**。
  已改为「退出码只表示这次运行有没有做完」，质量门结果留在输出的 `quality` 里。
- **缺口看得见了**：`shadow_job_health.py` + `ShadowJobs.incomplete_jobs`，
  `failed`/`running`/**无记录**都报；**挂进看护**（近 3 天、不含今天）。
- **缺口补得上了**：`ShadowJobs.claim(force=True)` + `retry_shadow_job.py`。
  `force` 只放开「重试耗尽」与退避，**不放开「已经成功过」与「仍在跑」**。
  实测 `selection_outcomes @ 2026-09-18` **补跑成功**（洞已关掉）。
- **仍不可补的**：`daily_setup_shadow @ 2026-09-18` —— 它撞的是
  `IMMUTABLE_SNAPSHOT_CONFLICT`，**历史快照已在，重跑在结构上被正当拒绝**。
  那 8/9 个标的的工作其实已经做完了，只有退出码说"失败"。
