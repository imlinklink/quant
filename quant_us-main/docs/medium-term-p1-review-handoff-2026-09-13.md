# P1 代码复核与继续推进交接（2026-09-13）

## 执行口径

本轮以 `medium-term-answer-first-runbook-2026-09-13.md` 的 P0→P1→P2 顺序为准。
旧动量基准手册仍是后续设计，不能越过 P1 放行门。P0 已完成；接手时工作区已有
P1/P2、行动覆盖和择时代码，以及 P1-001…006、P2-001…003 的本地产物，文档尚未同步。
这些未提交代码已保留并在其上修复，没有清理、覆盖旧结果或变更 HEAD。

当前 HEAD：`92ae0cd`；工作区有修改。本轮是探索性工程复核，不是干净提交冻结的正式实验。
各新 run 的 manifest 保存完整 commit、dirty 状态、输入及中期模块 SHA-256。
前向 `FWD-SURV15-20260914-003` 不在本轮执行范围，不能拿当前工作区直接冒充其冻结环境。

## 已修复并验证

- P1 不再先按理论仓位删除候选，再让现金账户二次录取。每个期限、每个子集都由实际
  账户沿时间线决定成交；零股数失败不会占用虚假仓位，剔除 BAC/HD 后重新分配空位。
- 显式传入共同评价起点，晚入场组在之前保留现金日，不再因首笔成交时间改变 CAGR 分母。
- 回撤高水位包含初始资金；首日收益进入波动统计。不足一年不输出 CAGR；基准缺日直接报错。
- 全部入场模式保留右删失矩阵行，质量截止后数据不得影响是否止损。未退出持仓进入期末
  市值，不能记为已退出胜负。成交和拒绝事件带 entry_id，可核对分母。
- 行动覆盖核对相同日期的比率/现金额对应调整因子，而非仅认“日期上有记录”；缺失价格、
  重复日期、错误比率均有拒绝路径。该核对仍依赖现有 Futu 数据和 0.5% 容差，不是独立来源证明。
- 禁止加载未绑定输入/代码哈希的旧矩阵缓存；新运行保存 prepared entries、退出矩阵、
  每日净值、成交、拒绝、摘要以及行动审计和 QFQ 分区哈希。
- QQQ 明确标为“原始收盘价格参考，未计分红、未扣成本”；不再把差值称为同口径超额收益。

## 本轮结果

`backtests/medium_term/P1-ACCOUNT-20260913-007/` 是配对主口径：
3,573 笔 A 入场 → 3,497 笔共同完整窗口；62 笔不足120日、14笔超质量截止。
五个期限各有3,497笔候选，共17,485个矩阵单元；共同评价日期2016-01-05至2026-08-19。
账户固定10万美元、1%初始风险、20%单票入场市值上限、最多五仓、整股、单边0.1%成交费率。
20%是建仓约束，不是价格上涨后强制每日再平衡上限。

| 全15只 | CAGR | 最大回撤 | Calmar |
|---|---:|---:|---:|
| 20日 | 8.17% | 18.39% | 0.44 |
| 40日 | 8.17% | 24.67% | 0.33 |
| 60日 | 12.50% | 26.16% | 0.48 |
| 90日 | 13.80% | 23.99% | 0.58 |
| 120日 | 7.62% | 31.74% | 0.24 |

90日相对40日改善账户收益和Calmar，120日没有延续P0单笔收益的优势；不能据此将90日认定为
未见样本最优参数。tech13是排除BAC/HD的说明性子集，并非独立核实行业分类的科技股票池。
单笔净收益栏仍是旧矩阵的“毛收益减0.2%”近似；账户净值使用实际买卖金额逐腿扣费。

`P1-ACCOUNT-20260913-008` 用全部3,573笔入场独立重跑，保留末尾未退出持仓，作为删失敏感性结果。
矩阵17,865行，共同评价日期2016-01-05至2026-08-31；全池20/40/60/90/120日实际成交
614/379/292/216/182笔，期末右删失分别0/0/1/1/2笔，CAGR分别9.69%/8.84%/13.52%/14.02%/8.59%。
科技子集对应右删失0/0/1/0/2笔。两批账户审计与输入/代码哈希检查全部通过，
完整逐组分母见 `backtests/medium_term/P1-REVIEW-20260913-001/audit/result.json`。

## 验证与产物

在 `quant_us-main/` 执行：

```bash
python3 -m scripts.medium_term.p1_account_check --output-dir backtests/medium_term/P1-ACCOUNT-20260913-007 --denominator paired
python3 -m scripts.medium_term.p1_account_check --output-dir backtests/medium_term/P1-ACCOUNT-20260913-008 --denominator all
python3 -m pytest -q
git diff --check
```

全量回归：**661 passed，18 warnings，退出码0**。日志：
`backtests/medium_term/P1-REVIEW-20260913-001/audit/pytest.log`。
新增用例涵盖初始亏损、基准缺日、子集重新录取、共同起点、截止后止损不影响删失、错误拆股比率和缺失复权价格。
账户审计验证现金+市值=净值、无融资、最多五仓、共同日期、候选=成交+拒绝、成交=退出+右删失。
两次P1命令和 `git diff --check` 均退出码0；既有产物未覆盖，未生成订单。

旧P1/P2目录保留。旧科技子集结果受仓位预筛影响；旧QQQ超额不属同口径。
P2代码还存在原始序列直接构造跨拆股特征、候选缺失/过期分母和共同评价窗待审计问题，
其既有结果只作诊断，不作为放行证据。

## 下一步

P1账户工程已推进；完整P1验收与P2仍未放行。下一步先独立核实QQQ分红与拆股记录，
冻结来源、日期和哈希，按相同开盘入场、逐腿成本、现金流和共同日期重建基准。
通过后重新生成新run-id的P1报告，再复核P2入场特征as-of口径及候选漏斗。
不能直接重跑已有007/008目录，不能将等权满仓（放开1%风险）替代本轮主资金约束。
