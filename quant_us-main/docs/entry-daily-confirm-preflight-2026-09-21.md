# H-A 开算前审计（2026-09-21）

状态：ENGINEERING_BLOCKED。本次没有计算两臂收益，没有改变预登记门槛，没有启动账户级实验。

依据技术设计 §15 P0 与 H-A 登记 early_stop_conditions：发现会计阻塞先修。新增 `scripts.strategy_diagnostics.entry_preflight`，对冻结输入哈希、登记基线身份、归档跨引擎检查和分红结算缺口执行检查；归档检查不是当前版本重新对拍。

## 实测阻塞

010 的 checks.json 明确列出支付日 2019-06-01、2019-12-01 的分红未支付，窗口内滞留应收共 239.715 美元。两个日期均为非交易日。现金仍计入 NAV，但未转成可用现金，可能影响后续定仓。两个引擎共享精确日期匹配逻辑，因此 VERIFIED 不能消除此缺陷。

这不是 H-A 的收益失败。需修复到期分红在首个可处理交易日结算（保留原支付日作为事件身份，确保重放和幂等），验证两套引擎，再冻结新基线。原 010 保留归档；用追加登记记录工程基线迁移及哈希，不能静默替换已登记的 study。

## 未算收益的样本核查

按预登记 candidate_round ≤ 2026-05-06，010 漏斗中共有 447 个 READY 候选：195 个账户成交、252 个账户拒绝。与预登记文字估计的 196 笔存在一笔差异，需按候选轮次与入场日逐项对账。

机会级总体不能仅取账户成交的 195 笔，否则会把容量筛选带入入场过滤消融。账户成交子集用于复现入场价、股数和止损的控制；全部 READY 用于机会级 A 臂。注册中的“成交集合”必须明确这一层次，B 的实际账户成交集合并不保证包含 A（容量和现金有路径依赖）。这些是开算前的定义问题，不得在看见收益后才修订。

## 验证与产物

新增 3 条测试全部通过：共享会计缺陷即使对拍 VERIFIED 仍阻断；干净检查可通过且不丢账户拒绝候选；缺少结算审计时拒绝放行。

机读结果：`preregistrations/ENTRY-DAILY-CONFIRM-20260921.preflight.json`，含登记、manifest、checks、漏斗文件 sha256。原登记文件未改。当前只交付前置审计，工程修复、新基线重跑和 H-A 收益计算尚未完成。
