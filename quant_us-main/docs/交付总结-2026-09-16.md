# 交付总结（2026-09-16）

本文总结 2026-09-16 至 09-17 的交付，按工作线分组。当前 HEAD `37f4853`，工作区干净、未 push。
背景与总体路线见 `return-drawdown-llm-roadmap-2026-09-15.md`。

## 1. 回测尺子 · P0-4 绩效复核

对应 roadmap §3.3 绩效冻结前核验清单。

| 交付 | 结果 |
|---|---|
| B3 逐日 as-of 一致性 | **证明不改代码**：入场信号（breakout/pullback/weekly regime）都是比较型，未来公司行动对整个回看窗施加同一乘法因子而抵消，故 `window[-1]` 锚定与逐决策日等价（3 条测试钉死） |
| 完整候选漏斗 | `candidate_funnel` 写入 P2 manifest，分母闭合 universe 1601 → selected 515 → timed{entered 461, expired 54, data_blocked 0, no_next_open 0} → prepared 442 |
| 等待窗按参考交易日历计数 | 修复 `timed_entries.py`（缺行情不延长等待期；对本 13 只科技股宇宙为 no-op，作正确性防线） |
| 缓存校验 / 基准分红边界 | 已满足（`_cache_signature` 哈希数据+行动+代码+参数）/ manifest 披露除息日记现金为研究近似 |

## 2. P1 同风险规则实验（冻结规则基线）

新增 `scripts/medium_term/risk_rule_experiment.py`。统一口径：13 只科技股 ×
单笔风险{0.5/0.75/1.0%} × 持有{60/90} + P1@20 参照 = 19 配置；union 评价窗
2016-01-05→2026-08-25；0.2% round-trip；$100k；入场日硬止损 + 五仓。

- **关键发现**：P1（固定 A 入场）整体被 B2/B3 支配（不在 Pareto 前沿）。
- **冻结基线 = B3@60 @ 单笔风险 1.0%**：CAGR 15.16%、MDD 13.9%、Calmar 1.09、
  top1 赢家占比 9.3%、win 53.7%；压力（单边 0.3%）MDD 14.3% 仍 <20%。
- 产物 `backtests/medium_term/RISK-RULE-20260916-001/`。

## 3. R/L 双影子账户（PR2/3/5/6/4）

新增 `scripts/portfolio_shadow/` 包。

| 批次 | 交付 |
|---|---|
| PR2+PR3 | `schema.py`（Manifest/Opportunity/AccountState + freeze 门）、`store.py`（独立 SQLite + 事件协议 + shadow 投影）、`paper_engine.py`（增量 EOD 引擎：int 微美元、T+1 结算、分红按 pay_date 应收、只消费当日已知信息）、`replay.py`（重放状态哈希一致）、`candidate_adapter.py`、`cli.py` |
| PR5 | `evidence.py`（Evidence Packet 双时间校验 + 质量分级 BLOCK/LLM_INSUFFICIENT/OK）、`llm_overlay.py`（`entry-veto-v1` 三态 PASS/VETO/ABSTAIN + 验证器 + FakeModel）、模型成本只记 L（`full_cost_equity`） |
| PR6 | `report.py`（日报 §8 看板 + 配对绩效 §9 L−R 全成本收益差/MDD 差/暴露差）、`evaluation_protocol` 完整性入 freeze 门 |
| PR4 | `risk_policy.py`（回撤阶梯 REDUCED/PAUSED_ENTRY/REVIEW_REQUIRED/LIMIT_BREACH + 迟滞恢复）、`hard_exit_router` 验收①② |

## 4. hard_exit 三验收项

| 项 | 交付 |
|---|---|
| ① 空 trade_id | `submit()` → `risk_anomaly/empty_trade_id`（不用于构造 exit_id） |
| ② 身份校验 | 持仓有 trade_id 且不匹配 → `risk_anomaly/trade_id_mismatch`（持仓无 trade_id 时跳过，兼容旧持仓） |
| ③ 端到端 DRY-RUN | `tests/unit/live/test_hard_exit_e2e.py`：tick → `_on_tick` → `HardExitRouter.submit` → `ExecutionService` → `apply_report` → 持仓冲正；硬止损不经确认台、部分成交/拒绝对账。**顺手修 bug**：`_evaluate_position` 产出 `HARD_STOP`/`TRAILING_EXIT`，与 `classify_exit_reason` 的 `fixed_stop`/`trailing_stop` 不对齐，导致 `hard_exit.enabled` 时硬止损仍走确认台；已加 `MONITOR_REASON_MAP` |

## 提交清单

```
37f4853 feat(portfolio-shadow): 信号预计算缓存 + 前向 bar 检查 + 历史引擎 shadow_precision 整数微定仓/费用
2fa7e45 fix(portfolio-shadow): 增量生成器接入质量区间/行动覆盖门（与 build_entries 一致）
642f707 feat(portfolio-shadow): CLI 前向运行循环 + 逐日增量信号生成（IncrementalCandidateGenerator）
7127013 feat(portfolio-shadow): 真实 LLM 接入（DeepSeek LLMAdvisor + entry-veto prompt + 成本）
947a00f feat(portfolio-shadow): 真实候选接入（月度动量+择时信号替换 fixture，与冻结矩阵 442 候选一致）
f968020 docs: 同步交付总结至第2轮 review + 第1条（历史/影子同口径）
9dd60a2 fix(portfolio-shadow): 机会/动作幂等写入 + 事件重放后继续 + 日历识别双边漏交易日
b7a8f59 feat(portfolio-shadow): 真实矩阵奇偶校验 + 修复除息止损未下调（历史/影子同口径）
f219888 feat(portfolio-shadow): 历史/影子引擎同口径奇偶校验（T+1 结算 + 分红应收旗标 + 逐日 diff）
a3fe4cd feat(portfolio-shadow): 恢复与幂等一致性（乱序拒绝/sequence 事务校验/估值缺口停延）
150703a fix(portfolio-shadow): 修复 review 5 项验收阻塞（回撤阶梯降级/幂等/holding_sessions 恢复/缺行情阻止开仓/MDD 首日损失）
a41d867 feat(live): hard_exit 验收项③ 监控器端到端 DRY-RUN + 原因码对齐
c64a4e7 feat(portfolio-shadow): R/L 双影子账户 + Evidence Packet + 回撤阶梯（PR2/3/4/5/6）
e93632d feat(medium-term): P1 同风险规则实验，冻结 B3@60@1% 基线
8319447 feat(live): 收盘任务闭环（P0-2/P0-3）+ hard_exit 空 trade_id/身份校验
f02e45f feat(medium-term): 回测尺子与 QQQ 同口径基准（P0-4 as-of/漏斗/日历/缓存 + A2/A4）
5448096 docs(strategy): 20% 回撤约束下的规则+LLM 双账户路线与详细设计
```

## 第2轮 review 修复 + 第1条（历史/影子同口径，2026-09-16）

| 问题 | 修复 |
|---|---|
| 重复写入机会/动作报「事件冲突」 | `store._insert_event` 改幂等（同 payload 已存在不再 raise；异 payload 仍冲突） |
| 事件重放后继续未验证到退出 | 新增「重放重建 → 继续步进直到 TIME_EXIT」对照测试 |
| 双方同时漏交易日无法识别 | `paired_performance` 增加 `calendar` 参数：按交易日历识别双边漏交易日 |
| 除息止损未下调（真实矩阵奇偶校验发现） | 影子引擎除息分支加 `stop_micro -= per_share`（镜像历史 `simulate_fixed_horizon_exits`），`replay` 同步 |

**真实 B3@60 全矩阵奇偶校验结果**（`verify_matrix_parity`，回撤阶梯关闭、仅比较核心会计）：

- 2625 个交易日、190 笔 entry；修复除息止损 bug 后，剩余差异为**浮点 vs 整数微美元精度**
  （max $37.30、mean $14.52，影子整数微更精确，属设计 §6.3 预期，非会计 bug）。
- T+1 结算、分红应收、除息止损下调三处会计已同口径对齐并验证。

## review 修复（5 项验收阻塞，2026-09-16）

| 问题 | 修复 |
|---|---|
| 回撤保护状态未经审查自动解除 | `risk_policy.evaluate_ladder` 改按严重度分级：只允许升级，降级走迟滞；REVIEW_REQUIRED / LIMIT_BREACH 不自动降级 |
| 同一交易日重复运行再次推进账户 | `paper_engine.step` 加 `last_session == session` 幂等 no-op（不推进 sequence/持有天数/结算） |
| 重放哈希排除 holding_sessions | 状态哈希包含 holding_sessions；引擎发 `hold` 事件，replay 折叠恢复（恢复后可继续运行） |
| 缺持仓行情仍可新开仓 | 入场前检查已有持仓缺 bar → `VALUATION_INCOMPLETE` 阻止新增风险 |
| 回撤报告漏算首日损失 | `report._max_drawdown` 高点含初始资金；配对绩效排除 PROVISIONAL 并计数 |

## 状态（最终）

交付状态：**「同风险研究完成；R/L 双影子账户（真实候选 + 真实 LLM + CLI 前向循环）原型完成；运行一致性/恢复验收基本闭环；尚未开始正式前瞻入组。」**

- 全量测试 **799 passed**；工作区干净、未 push。
- **B3@60@1% 的 15.16% CAGR / 13.90% MDD** 来自 `risk_rule_experiment` 一次运行，能与历史报告对应，
  但**本次未重新运行完整回测**；该数字是冻结候选，非已完成的前瞻验证。
- 真实候选（`build_real_schedule` + `IncrementalCandidateGenerator`）与冻结矩阵一致（442 候选、226/226 对拍一致）；
  真实 LLM（`RealModel`，DeepSeek）已接入（测试用 mock advisor）；CLI `run-forward` 逐日循环已交付。
- 历史/影子会计已在**现金结算（T+1）、分红（应收）、除息止损下调**三处对齐并逐日校验。
- **已知边界（如实）**：① 历史引擎现金累积仍为 float，与影子整数微有 ~$10-37 精度差（0.002–0.008%，
  影子更精确，严格零分歧需整体资金循环整数微化，已决定不做）；② 增量生成器前向 bar 检查用策略 horizon(60)，
  冻结矩阵用 `max(HORIZONS)=120`（多持仓期产物，约 20 条差异）；③ 全量回测对拍 ~130s（前向逐日够快）。
- **仍不宜标记为「可以开始正式前瞻入组」**：需预登记样本/观察期 + 实际逐日跑（运营动作），并补齐上述边界。

后续：正式前瞻入组（预登记样本/观察期）→ 实际逐日运行 → 到期绩效评审。
