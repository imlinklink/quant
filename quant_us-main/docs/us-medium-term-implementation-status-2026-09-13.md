# 美股中期动量实验工程进度（2026-09-13）

实验编号 `US-MT-MOM-BASELINE-001`，协议仍是 `draft_requires_data_freeze`。此阶段只做输入与交易机制联调，不发布 B1/B2/B3 收益，不能把它作为已验证策略。

## 已完成

- 八只 ETF（IGV、IWM、QQQ、SHY、SMH、SOXX、SPY、XLK）的 Futu 原始日线与 QFQ 日线各 96 个年度分区；每只 2,940 个交易日，覆盖 2015-01-02 至 2026-09-11。原始输入在 `data/medium_term/US-MT-MOM-BASELINE-001/market_history/`，无覆盖共享历史目录。
- ETF 输入审计在 `data/medium_term/US-MT-MOM-BASELINE-001/prepared/etf_input_audit.json`。原始日线 SHA-256 为 `546b0b74c860132a7dd15a861f15772982bb8de50732d9eaac3feb3a83a0fe82`；QFQ 为 `6f3a738ff061c97a483bf1c1b72b1d8416220936ed8e33e3f1551aba712853c5`。本轮新增的候选异常字段改变了行动候选文件格式，必须重建新审计版本，不能沿用旧行动文件哈希。
- B1/B2 动量、月末日历、B2 候选、M1–M5 退出、组合资金与成本会计、绩效函数已有基础实现和本地 fixture 测试。按决策日重建完整回看序列的函数处理拆股，避免把逐日锚定的 `asof_close` 直接相除。信号日 ATR 与行动尺度转换后才计算下一日原始价止损。
- 周线退出特征只在完整交易周末标记；中途缺一个交易日或样本尾部不足一周均不能触发。对有拆股/分红的证券，周线均线尚须在每个完整周的 as-of 视图重建后才可接入正式退出。

## 放行阻断

1. Futu 公司行动导入对八只 ETF 返回 **0 条**，故中期实验停用该导入步骤；旧导入脚本仍供其他流程使用，不作为本实验依赖。QFQ/原始价差只推导 31 个**未经验证的诊断候选**：QQQ 和 SHY 均为 0，IGV 有 5 天内同额分红候选，XLK 有 ratio=1 的反向拆股候选。候选不能直接作为真实现金流。正式收益计算仍须独立取得并核对逐次除权、分红记录，记录来源和时间，冻结可审计行动表；随后重新生成 as-of 特征与输入哈希。
2. 当前股票主数据没有可靠的 `sector/industry`，不能先看收益再手选科技股票。需独立来源的科技普通股分类、当时上市状态、流动性资格和冻结名单；用户选择不纳入退市证券，报告必须注明存续样本偏差。
3. 当前 B1/B2 的兼容调用允许传入普通逐日价格序列，但**正式运行必须传入经核实的公司行动和原始 OHLCV**，并使用按决策日重建回看序列的调用路径。现有逐日面板 `asof_close` 不能作为跨日动量价格直接相除。正式 runner 应检查协议状态、输入质量门和源文件哈希并拒绝不合格输入。
4. M4/M5 的完整周 20 周均线必须在公司行动口径下逐周 as-of 构造；B3 周线准入和突破/回踩等待逻辑尚未完成。运行器、基准对齐、报告和实验冻结机制仍待开发。

## 下一步顺序

不再运行 `import_corporate_actions_from_futu.py` 作为本实验的前置步骤。价差候选仅供诊断。先独立核实 ETF 公司行动并重建审计文件；并行准备科技普通股分类和冻结名单。然后补全周线 as-of 特征、B3 入场、正式运行质量门与组合级报告。冻结协议、输入哈希、Git commit 和主判据后，依次运行开发/验证/测试区间；测试期只运行一次。任何规则和数据修改都另起实验编号。
