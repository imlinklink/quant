# 离线部署验收（2026-09-08）

本轮审查和隔离演练通过；尚未进行真实 OpenD 模拟账户联调或运行库部署。

## 审查修复

- 部分成交状态不变时，确认页仍更新累计成交数量；重启后从持久化提案恢复最新进度。
- 旧数据库迁移通过进程文件锁串行执行，在首次修改表结构前生成完整 SQLite 备份并校验；原 books 数据保留。当前文件锁实现使用 Unix fcntl，本轮在 macOS 验证。
- 已终结订单再次报告新增成交时，拒绝静默修改冻结 R0，要求核对券商更正。此时仍需人工或后续更正流程处理，不能视为已自动对账完成；相同成交量的费用更正仍可处理。

## 演练结果

执行 `python3 scripts/run_deployment_rehearsal.py`：76 项测试通过，0 失败、0 错误、0 跳过。包括旧表并发迁移与原始备份校验、部分成交后重启恢复、模拟券商提交响应丢失后只查询不重复下单、模型失败/评估过期阻止批准、outbox 导出失败重试。

原始 `data/approvals/decisions.jsonl` 为 0 行，`data/decision_ledger/signals.jsonl` 为 24 行。两份独立副本均通过 SHA-256 校验，JSONL 无解析错误，演练结束后原文件哈希不变。副本仅供保存历史，不补造旧事件与新版决策的关联。

报告保存在 `data/deployment-rehearsal/20260908T063105172902Z/report.json`；同目录保存测试日志、JUnit XML 和历史副本。这些本地数据不纳入 Git。再次执行脚本会创建新的独立目录。

## 尚未验证

本地没有 `data/execution.sqlite3`，因此没有执行真实旧库副本迁移；迁移测试使用合成旧版 books 表。没有连接真实券商、启动交易进程、重启服务或修改交易参数。真实部署前仍需在目标机器停止相关写入，备份并验证实际运行库，以及使用 OpenD 模拟账户验证适配器返回字段和恢复行为。
