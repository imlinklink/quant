# quant 双系统审查与修复清单

> 范围：2026-09 多轮业务层审查（港股 quant_futu-main + 美股 quant_us-main），
> 覆盖交易/持仓/资金、风控退出、人工确认、LLM、数据与存储、回测、评估闭环、工具脚本。
> 本清单按「提交」与「根因」两个维度整理，便于回归与复盘。

## 一、提交索引（按时间）

| Commit | 主题 | 主要修复 |
| --- | --- | --- |
| `1a8ddcc` | 港股持仓并发安全与卖出评估口径 | broker sync 加锁、manual/demo 不回冲、`_selling` 防重入；账本 side 字段、回填/周报排除卖出；归因 `__all__`、停牌过滤；回测净值初始化；清理脚本按 env |
| `f59f03b` | 实盘退出状态链路与资金口径 | buy_date 落库并持久化；退出检查复用持久持仓（RSRS/时间退出在实盘复活）；冷却期/最高价重启恢复；save 路径 manual 不计资金；无锁读改快照；日内K线失败不缓存 |
| `8b9b622` | 部分成交/撤单/YAML/风控配置接线 | 部分成交保留余量；确认超时必撤单并回查；YAML 损坏抛错+原子写；risk 配置真正接入退出策略；冷却期原因扩围；空持仓不降级；lot_size None 语义与实盘跳过；CLI 解包修复 |
| `d6f615b` | 港美股识别/简报日期/lot_size/缓存防覆盖 | 美股现金列/持仓按 market 识别；简报自带日期与 schema 归位；数据层 lot_size None 防护；手动/策略仓按 trades 判定；K线空数据不覆盖缓存；演示 LLM 类型一致 |
| `c2d863b` | 节假日/限流/简单窗口/昨收/SecurityType | 富途交易日历接入（节假日不再误交易）；限流器窗口只删过期（港+美）；简单买入窗口可配置；昨收按日期区分；futu.SecurityType 别名防覆盖 |
| `8439f32` | 回测/归因/新股识别边界 | 杠杆缺口日志口径；find_best_strategy 空值/长度防护与 max_drawdown 缺失语义；新股判断使用回测日期 |
| `a72e371` | 美股条件单状态机 | 止损单缺失补挂；数量变化触发改单；worker 对账撤销残留条件单 |
| `0f45cbb` | LLM 退避/脏数据 | LLM 重试退避 0.5s；jsonschema 显式依赖；趋势检测剔除 NaN/无效基准 |
| `c0bb61d` | dual_chandelier 止损 ratchet | ATR 大盈利模式激活后不再回退固定%/保本，跳空保留高位止损 |
| `2791668` | 提案留存清理 | proposal_store 终态 24h 清理（港+美）；死代码清理 |
| `6e92741` | 观察池写入安全 | watchlist 读-查-写加锁 + 原子写 |
| `03b2c86` | 港股买入循环日历闸门 | 周末/节假日不再触发选股/买入提案 |
| `f941719` | 退出兜底/费用/超时 | UnifiedExitStrategy 缺失方法补齐；买入手续费从策略资金扣除；partial_timeout 可配置 |
| `6d1572f` | 简报日期闸门/部分成交撤单 | avoid 仅当日生效（三条买入线）；撤单 OrderError 时返回实际成交 |
| `ebf246e` | 选股结果落盘 | code/name 与 stock_code/stock_name 双写；selection_results 按 env 覆盖 |
| `61b7009` | 日内打分修复 | 追涨配置读 analysis.momentum；「跌后企稳」死分支修正；max_positions 截断；日内K线 futu host；追涨量价阈值接线；RSI 注释校正 |
| `ef43de8` | 简报 schema | 新增 MARKET_BRIEF_SCHEMA，修复简报永远生成失败 |
| `67c347c` | 工具脚本迁移/防护 | new_ipo 扫描迁移 YAML；chart_generator 去 DB 死路径；picker confidence 防护；进程 start_time；Makefile 去硬编码 |
| `5b860c7` | view_selection env | 选股结果/持仓查看按 config env（默认 SIMULATE） |
| `d59a29b` | 日内K线并发/恢复脚本 | OpenQuoteContext 请求同锁、连接复用；持仓恢复脚本备份+保留 buy_time/manual/highest |
| `eb7ba8f` | momentum_analysis 迁移 | 全脚本 MySQL→YAML+富途 |
| `45e0903` | 尸体代码风险 | live_position_manager.sync_with_broker highest_price≥成本并标注未启用 |

## 二、按根因分类

### 1. 并发 / 线程安全
- `sync_with_broker` 无锁改持仓/资金 → 全程持锁，锁内重校验
- `_selling` 只写不读 → 加防重入（执行入口 + 检查循环 + sync 跳过）
- 多处无锁 `strategy_positions.keys()/items()` → `snapshot_position_codes/positions` + 兼容回退
- 美股日内 K 线 OpenQuoteContext 请求在锁外 → 选择与请求同持锁、连接持久复用
- 观察池 config 读-查-写 → 模块锁 + 原子替换
- proposal/approval 状态机原子转换、幂等（确认与超时兜底不会双卖/双买）

### 2. 订单 / 成交边界
- 部分成交后本地整仓删除 → 保留余量继续监控，整仓才 pop + 冷却
- 确认总超时遇部分成交不撤剩余单 → 一律先撤单再回查最终成交
- 撤单抛 OrderError 丢失已成交量 → 按实际成交返回，避免券商有仓本地无记录
- partial_timeout 硬编码 → 配置化透传
- 美股条件单：撤旧挂新失败止损永久缺失 → 补挂；qty 变化不同步 → 触发改单；
  券商已无仓残留条件单 → worker 对账撤销
- 卖出确认状态机（pending→approved→executing→executed/failed）经人工复核无重复执行

### 3. 资金 / 账目口径
- 买入手续费从未入账 → 买入即从 strategy_capital 扣除（佣金/征费/结算/滑点）
- manual 仓被 save/fallback 计入 used_capital → 统一不计入（load/sync/exit 同口径）
- 同步移除 manual/demo 误回冲资金 → 跳过
- 回测平仓日志 gross/net 展示口径（待决策项）

### 4. 状态丢失 / 持久化
- 买入未记 buy_date、退出检查用临时 dict → buy_date 落库、退出复用持久持仓，RSRS/时间退出在实盘复活
- 重启丢 highest_price / buy_time / 冷却期 → positions 表与 trading_state 持久化并在 Futu 同步时合并
- YAML 解析失败返回空表导致整表覆盖 → 失败抛错禁止覆盖；保存临时文件 + os.replace 原子写
- selection_results 整表覆盖 env → 按 env 隔离覆盖
- 持仓恢复脚本清空式重建 → 先备份；导入保留 manual/buy_time/highest_price

### 5. 配置接线 / 字段一致性
- risk 段从未传入退出策略 → `config['risk']` 扁平化，yaml 风控参数生效
- 追涨打分读错段 → `analysis.momentum`
- 日内K线 host/port 读错 key → `trading.futu.host/port`
- view_selection 硬编码 REAL → 从 config env 解析
- 简报 schema 与 prompt 字段不匹配 → 新增 MARKET_BRIEF_SCHEMA
- 选股结果 code/name vs stock_code/stock_name 不一致 → 双写
- 美股现金列/持仓按市场识别（hk_cash vs us_cash）
- 输出 schema 校验与各接入点字段保持一致

### 6. 时间 / 日历 / 缓存
- 港股只判周末 → Futu 交易日历（年度缓存、失败降级）
- 港股买入循环缺日历闸门 → 补 `_is_trading_day`
- 简报 avoid 不校验日期 → 仅当日生效（港主循环 + 美股 dip/trend）
- 简报生成不带 date/generated_at → 自带并 load 合并默认字段
- 昨收：只有今日一根不冒充昨收，盘前昨收正常返回
- 简单模式买入窗口 60 秒 → 可配置（默认 10 分钟）
- K 线空结果覆盖全天缓存 → 保留旧缓存、不推进更新日期
- 日内 K 线失败缓存 None → 不缓存下轮重试

### 7. 死代码 / 历史迁移（MySQL→YAML）
- new_ipo_momentum_scan → YAML + 富途
- momentum_analysis → YAML 股票池 + 富途 K 线
- chart_generator 本地 DB 死路径 → 统一富途
- live_position_manager / backtest_position_manager：标注未实际启用并修内部风险字段
- run_manager.py 遗留入口：确认生产入口为 run_all.py（未删除，建议后续清理）
- `MomentumStrategy.position_manager` 创建后未使用（记录待清理）

### 8. 数据健壮性 / 降级
- lot_size None 不再默认 100：数据层安全回落，实盘下单路径跳过该票
- hk_market_rule/hk_fetcher 对 None/空字段防护，避免 `int(None)` 与除零
- 富途错误串不再当数据（fetch_stock_kline 失败置 None 走兜底——复核确认已修）
- 趋势检测剔除 NaN 脏 K 线、基准价≤0 判震荡
- 限流器窗口到期只删过期时间戳（港+美），防 429
- futu.SecurityType 别名防覆盖（base_fetcher 同名枚举）
- `_calc_candle`「跌后企稳」条件互斥死分支 → 修正

### 9. 评估 / 展示口径
- 卖出提案/人工决定/执行事件不再混入买入侧回填与周报统计（side 字段）
- 归因分组前补 `__all__` 列；回填过滤停牌日
- find_best_strategy 空输入/长度不一致防护；max_drawdown 缺失按最差

### 10. LLM / 容错
- 所有接入点失败/超时/格式错降级纯规则（复核确认）
- LLM 重试退避不阻塞交易热路径
- picker confidence 非数字/null/越界回退默认
- candidate_review veto-only（只删不增）约束成立
- 简报/建议生成失败不影响交易主流程

### 11. 运维 / 编排
- run_all.py SIGINT/SIGTERM 优雅退出顺序（先买入后出场）——复核确认
- process_manager start_time 取真实进程时间
- Makefile 去硬编码 PYTHONPATH
- 卖出确认超时提示展示真实 TTL/到期时刻

## 三、验证与回归
- 现有测试入口（港股侧）：
  - `tests/unit/live/test_round5_fixes.py`（并发/退出/限流/提案/费用等 24+ 条）
  - `tests/unit/live/test_intraday_fixes.py`（追涨配置/企稳/量价阈值）
  - `tests/unit/strategies/test_unified_exit_fallback.py`（兜底路径）
  - `tests/unit/llm/test_market_brief_schema.py`（简报 schema）
  - `tests/unit/live/test_picker_confidence.py`
- 已知既有失败（与本轮修复无关，建议单独处理）：
  `test_live_manager_base.py::test_do_selection_exception`（fetcher 异常在 `_get_watch_pool`
  内被 warning 吞掉，测试却期望 error）

## 四、遗留待决策 / 待办
1. **吊灯止损基准**：实现为 `min(period_high, cost)` 且以成本价为上限（docstring 已按实现校正）；
   若要"随最高价上移锁盈"，需按 max 语义改逻辑并回测验证——需你拍板。
2. **回测口径**：T+1 开盘成交但指标 df 含当日行（潜在轻度前视）；平仓日志 gross/net 展示差异。
   改动会改变历史回测结果，需确认后再动。
3. **尸体代码清理**：`mutifactor/position` 双管理器、`run_manager.py`、`MomentumStrategy.position_manager`
   建议后续加 deprecation 或删除。
4. **config 校验扩展**：把历次踩坑的 key（risk/momentum/analysis.momentum/trading.futu/env 等）纳入
   `config_validator` 启动必检，防止配置错位复发。
5. **运行时验证**：SIMULATE/DRY-RUN 连续跑一周 + 每日对账 + 周末演示全流程（推荐下一步）。
