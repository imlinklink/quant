# H-A 诊断结果：移除日线确认（`ENTRY-DAILY-CONFIRM-20260921`）

- 日期：2026-09-21
- 预登记：`docs/preregistrations/ENTRY-DAILY-CONFIRM-20260921.json`（sha256 `9c9a2a97…`）
- 登记修订：`.engineering-amendment.json`（sha256 `57b97c7a…`，`returns_examined: false`）
- 基线：`SD-P0P1-20260921-011`（010 的迁移版；跨引擎对账 VERIFIED、窗口内到期应收为 0）
- 机读结果：`.result.json`（含逐候选两臂）
- 复算：`python -m scripts.strategy_diagnostics.entry_diagnostic --study data/strategy_diagnostics/SD-P0P1-20260921-011 --out <path>`

---

## 结论：`CONCENTRATED` —— **保留原日线确认**，不进账户级

登记的门槛里，**集中度两条都不通过**：配对增量 **75.8% 来自 NVDA 一只**，
前两只（NVDA + NFLX）合计 **101.3%**（≥总增量 ⇒ 按登记视为"集中于两只"）。

**而且不止集中**：主指标的 95% 区间在三种块长下**都跨零**
（10 会话 `[−0.101, +0.232]`、20 会话 `[−0.095, +0.235]`、40 会话 `[−0.104, +0.219]`）。
即："移除日线确认能改善扣费后入场收益"这句话，**既不稳、也不显著**。

## 控制项（都通过，否则结果不可用）

| 控制 | 结果 |
|---|---|
| **成交复现（双向）** | 归档 201 笔：**195 笔逐字段匹配**（入场价与止损），**6 笔由预登记的候选轮次截止解释**，无一笔无法解释 |
| A 臂复现 010/011 的成交 | 195 笔入场价与止损全部相等（`BASELINE_ENTRY_OR_STOP_MISMATCH` 未触发） |
| 010 → 011 成交一致 | 逐笔相同（结算修复未改变交易结果） |
| 次一交易日开盘成交 / 止损日 MAE 只计到实际退出价 | 有引擎级测试钉住 |

> **这处控制原先只做单向**（"对得上才比"），重建漏掉真成交时会静默跳过。已改为双向：
> 对不上的一律要求由候选轮次截止解释，否则立即失败；轮次查不到也失败。
> 反证：退回单向后对应测试立刻失败。

## 主指标与风险门槛

配对总体 = **447** 个合格候选（登记修订后的口径：全部 READY，各给独立 10 万美元空仓起点、
原 100bp 风险与 20% 权重上限，不是只看账户成交子集）。

| | 基线 A（含日线确认） | 实验 B（仅周线门） |
|---|---:|---:|
| 净 R 合计 | +377.44 | **+408.32** |
| 净 R 均值 | 0.8444 | **0.9135** |
| 止损出场率 | 46.5% | **42.3%** |
| `< −2R` 笔数 | 1 | 1 |
| 最差 5 笔之和 | −9.26 | **−8.49** |
| 净 R 分位 [min, p5, p50, p95, max] | [−2.05, −1.12, 0.03, 5.64, 12.46] | [−2.05, −1.07, 0.37, 5.68, 13.95] |

**配对 Δnet_R 均值 +0.0691R**，区间见上（跨零）。

**风险门槛全部通过**——实验臂并不更差：止损率更低、最差 5 笔更好、`< −2R` 相同。
所以本次不是"用风险换收益"被否，而是**收益本身不成立 + 增量集中在个别标的**。

⚠ **但不利波动那条门槛（MAE）没有信息量**：`mae_r` 被止损截断在恰好 1.0（STOP 按
`stop_micro` 成交 ⇒ `MAE = shares × (entry − stop) = risk`），两臂都是 1.0 ⇒ 配对中位数差
恒为 `[0.0, 0.0]`，形式上通过、实际无法区分。**下一次若要用它，应改用未截断的不利波动。**

## 增量按证券（配对 Δnet_R 合计 +30.88R）

| 证券 | ΔR | 占比 |
|---|---:|---:|
| SEC-US-NVDA | **+23.41** | **+75.8%** ← 唯一大头 |
| SEC-US-NFLX | +7.88 | +25.5% |
| GOOGL / MSFT / AMZN / MU / META / ARM / LITE / AAPL | +0.04 ~ +2.85 | 各 ≤ 9.2% |
| SEC-US-AMD | −5.44 | −17.6% |
| SEC-US-INTC | −8.65 | −28.0% |

13 只里 **2 只为负**；`leave_one_out`（剔除 NVDA 后）= **+7.47R**。

## 两个方向（§5.4）与仅实验侧

仅实验侧成交（周线门曾成立、窗口内从未触发日线信号）= **18 个候选**：

| | 笔数 | 净 R |
|---|---:|---:|
| 基线错过、实验侧**赚** | **1** | +5.83 |
| 基线避开、实验侧**亏** | **17** | **−17.52** |

该批净 R 均值 **−0.649**，止损出场率 **94.4%**。

> 这是本次最直白的一条：**日线确认的价值恰好体现在它排除掉的那些候选上** —— 少了它就会
> 多进 18 个，其中 17 个亏。该批只有 18 组（< 登记的 30），故按登记标为样本不足，
> 但它与"集中度不通过"指向同一结论。

## 机制指标：假设的前提**未被支持**

- **确认溢价**（实验臂入场价相对**周线门首次满足日**收盘）：均值 **−0.02%** —— 几乎为零。
  即"等日线确认 = 买得更贵"这条机制在本窗口上**没有出现**（登记已写明它只作机制指标）。
- **入场延迟**（基线 − 实验，会话数）：均值 **3.4**、中位 **2**、最大 19；其中 **124/447
  延迟为 0**（两臂同日入场）。

## 政策层面（两臂**全部**成交，含仅实验侧）

| | 笔数 | 净 R 合计（1×） | 净 R 合计（2×） |
|---|---:|---:|---:|
| A（含日线确认） | 447 | +377.44 | +366.97 |
| B（仅周线门） | 465 | **+396.63** | **+385.78** |

B 多进 18 个（净 −11.68R）、配对臂多赚 +30.88R，合计 +19.2R。**这不是账户结果**
（未建模容量与路径依赖），只作政策层面的记账，登记里已明确禁止拿它替代配对。

## 为什么没进账户级

登记写明：机会级须 `SUPPORTED`（主指标区间下界 > 0 且全部风险门槛通过）才做账户级。
本次集中度不通过、区间跨零 ⇒ 判定 `CONCENTRATED` ⇒ **账户级未启动**
（`account_level_started: false`）。

## 限制（必须与结论一起读）

1. **不利波动的门槛没有信息量**（见上，MAE 被止损截断）。故"没有恶化不利波动"这句
   在本报告里**只是没有证据显示恶化**，不是"已检验过没恶化"。
2. **仅实验侧只有 18 组**（< 30）⇒ 那一批的统计结论标为样本不足，只作方向性参考。
3. 历史窗口 `2015-01-02 → 2026-08-31` **已被查看 ⇒ 只能定位问题，不得作为验证**；
   正式资格需事先登记的前向窗口（≥30 独立成熟组且 ≥60 交易日）。
4. 本次尝试计入试验登记且**不删除**；与 H3 一样，失败方案保留。

## 登记中未启用的部分

- 账户级验证（机会挤占与回撤）**未启用**；
- 前向验证窗口**未启用**（无候选规则可验）；
- 未产生任何 challenger、未改任何规则。
